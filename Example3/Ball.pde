// Create a ball class.
// Each ball has an x, y, speed and color (c)
//
// This is just the first go of the class
// It will become much more useful later.

class Ball {
    public int x;
    public int y;
    public int speed;
    public int c;  // can't use the word "color" because that is a command.
};
